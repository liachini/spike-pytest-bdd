[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pytest = "*"
pytest-bdd = "*"
paho-mqtt = "==2.1.0"

[dev-packages]
black = "==25.11.0"
flake8 = "==7.1.1"
flake8-pyproject = "==1.2.3"
pytest-html = "==4.1.1"
pytest-cov = "==6.0.0"

[requires]
python_version = "3.10"

[scripts]
test = "pytest"
# Questo comando apre il report usando il browser predefinito del sistema operativo
report = "python -c \"import webbrowser, os; webbrowser.open('file://' + os.path.realpath('reports/report.html'))\""
# Anche per la coverage puoi fare lo stesso
coverage-report = "python -c \"import webbrowser, os; webbrowser.open('file://' + os.path.realpath('reports/coverage/index.html'))\""